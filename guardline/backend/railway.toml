[build]
builder = "NIXPACKS"
buildCommand = "npm install --ignore-scripts && npx prisma generate && npx prisma db push --accept-data-loss=false --skip-generate"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
